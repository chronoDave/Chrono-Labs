import { mtx, Mtx } from 'mtx';

import { Block } from '../../components';

export class ContentLanding extends Mtx {
  view() {
    return (
      <Block fullHeight background="fade">
        Landing
      </Block>
    );
  }
}
