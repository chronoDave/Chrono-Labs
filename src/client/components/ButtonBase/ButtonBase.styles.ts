import { makeStyles } from '../../theme';

export default makeStyles(() => ({
  root: {
    outline: 0,
    border: 'none',
    cursor: 'pointer',
    backgroundColor: 'transparent',
    padding: 0
  },
}), 'buttonBase');
